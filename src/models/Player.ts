export class Player {
  name: string;
  position: string;
  overall: number;

  constructor(name: string, position: string, overall: number) {
    this.name = name;
    this.position = position;
    this.overall = overall;
  }
}